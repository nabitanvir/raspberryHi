commands i keep forgetting when i code
1. docker run -v "$(pwd):/usr/src/app" -it berry bash
2. docker build -t berry .
3. docker stop $(docker ps -a)
4. docker system prune -a --volumes
5. docker volume prune
